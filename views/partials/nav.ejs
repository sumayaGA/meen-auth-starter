
<nav>
    <!-- Update Nav Partial with Conditional Rendering and Logout Button (ASK ABIDUR what this if else statement is saying. I know it creates a logout button but I want to understand the if/else conditionals) --> 
    <% if(currentUser) { %>
        <a href="/">Home</a>
        <form action="/sessions?_method=DELETE" method="POST">
			<input type="submit" value="Log Out" />
		</form>
    <% } else { %>
        <a href="/">Home</a>
        <a href="/users/new">Register</a>
        <a href="/sessions/new">Login</a>
    <% } %>
</nav>